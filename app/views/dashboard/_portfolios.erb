
<%# Uses the passed variable "portfolios" and creates the buttons of each entry %>
<% portfolios.each do |f|  %>

    <%if params[:portfolio_id].to_s == f.id.to_s %>
        <%= link_to "#{f.portfolio_name}", dashboard_path(portfolio_id:  f.id), role:"tab", class:"tab tab-active"%>
    <%else%>
        <%= link_to "#{f.portfolio_name}", dashboard_path(portfolio_id:  f.id), role:"tab", class:"tab"  %>
    <%end%>
<%end%>


